extends layout

block content
  if user
    h1 Hi #{user}! Welcome to Tube Clone
  else
    h1 Welcome to Tube Clone
  a(href='/tube') To All Videos
  br
  if user
    a(href='/tube/new-video/' + userId) Add Video
    br
    a(href='/tube/user/' + userId) User Dashboard
